<div class="hero-section">
    <div class="overlay"></div> <!-- Superposición gris translúcida sobre la imagen de fondo -->
    <div class="content">
        <h1 class="title">Librería Web</h1>
        <h4 class="subtitle">¡En el conocimiento está el poder!</h4>

        <div class="welcome-section">
            <h2 class="welcome-title">Bienvenido a tu Librería de confianza!</h2>
            <p class="welcome-text">
                Donde puedes explorar y acceder a una amplia
                colección de libros digitales sobre diversos temas.

                <br><br>
                ¡Empieza a descubrir nuevos conocimientos e historias hoy!
            </p>
        </div>
    </div>
</div>

<style>
    .hero-section {
        position: relative;
        min-height: 100vh; /* Ocupa la altura completa de la ventana */
        background-image: url('@Url.Content("~/imagenes/library.png")');
        background-size: cover;
        background-position: center;
        background-attachment: fixed;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    .overlay {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-color: rgba(0, 0, 0, 0.6); /* Fondo oscuro translúcido */
    }

    .content {
        position: relative;
        z-index: 1; /* Asegura que el contenido esté sobre la superposición */
        text-align: center;
        padding: 2rem;
        color: white;
        max-width: 800px;
        margin: 0 auto;
    }

    .title {
        font-size: 3.5rem;
        margin-bottom: 1rem;
        color: white;
    }

    .subtitle {
        font-size: 1.5rem;
        margin-bottom: 2rem;
        color: rgba(255, 255, 255, 0.9);
    }

    .welcome-section {
        background-color: rgba(128, 128, 128, 0.8); /* Caja gris translúcida para el texto de bienvenida */
        padding: 2rem;
        border-radius: 10px;
        margin-top: 2rem;
    }

    .welcome-title {
        font-size: 2rem;
        font-weight: bold;
        margin-bottom: 1.5rem;
    }

    .welcome-text {
        font-size: 1.2rem;
        line-height: 1.6;
    }
</style>
